import { Scroll } from 'scrollex'
import { chakra } from '@chakra-ui/react'

export const ScrollItem = chakra(Scroll.Item)
export const ScrollSection = chakra(Scroll.Section)
export const ScrollContainer = chakra(Scroll.Container)
